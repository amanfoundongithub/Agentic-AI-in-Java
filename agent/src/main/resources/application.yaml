


# Spring configuration
spring:
  application:
    name: agent

  data:
    mongodb:
      host: localhost
      port: 27017
      database: agentic_ai

  config:
    import:
      - "application-secret.yaml"
      - "application-tool.yaml"
      - "application-llm.yaml"


# Server configured to port 6900
server:
  port: 6900

# WebClient configurations
web_client:
  timeoutInSeconds: 20